#include <iostream>
#include <chrono>
#include <cstdlib>
#include <cstring>
#include "MaxSubSeq.h"

std::vector<int> llenarVector(int numPeriodos);

int main( int argc, char* argv[] )
{
    int numPeriodos;
    std::cin>>numPeriodos;
    
    
 
std::vector<int> llenarVector(int numPeriodos)
{
    int num;
    for(int i = 1; i <= n; i++)
    {
        num = (rand() % 999 + 1);
    }
}
